var numbers = [1, 9, 27, 5, 10, 3];

// each is for iteration
function each(array) {
	fpr (var i = 0; i < array.length; i++) {
		sum = sum + array[i];
	}
}

// addToSum does the work
function addToSum (number) {
	sum = sum + number;
}

each(numbers );